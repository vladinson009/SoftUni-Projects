function highJump(input) {

  let neededHeight = Number(input[0]);
  let currentHeight = neededHeight - 30;
  let jumps = Number('');

  for (let i = 1; i < input.length - 1; i++) { //jumpHeight
    let currentJump = input[i]

    // console.log(`Tihomir failed at ${височина на летвата към момента на провала}cm after
    // ${брой скокове от началото на тренировката} jumps.`)
  }

}


highJump(["231", "205", "212", "213", "228", "229", "230", "235"])